# Auto Git Trend

Bootstrap for the Auto Git Trend Next.js app.

## Getting Started

1. Copy `.env.example` to `.env.local` and fill in the values.
2. Install dependencies with `npm install`.
3. Start the app with `npm run dev`.

## Useful Commands

- `npm run lint`
- `npm run build`
- `npm test`

## Environment Variables

- `SUPABASE_URL`
- `SUPABASE_SERVICE_ROLE_KEY`
- `GITHUB_TOKEN`
- `CRON_SECRET`
- `OPENAI_API_KEY`

The homepage is a placeholder for now. Trending snapshots will render here later.
