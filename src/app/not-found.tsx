export default function NotFoundPage() {
  return (
    <main className="page">
      <section className="hero">
        <p className="eyebrow">Archive</p>
        <h1>Snapshot not found</h1>
        <p className="lead">
          No archived GitHub Trending snapshot exists for the requested date.
        </p>
      </section>
    </main>
  );
}
